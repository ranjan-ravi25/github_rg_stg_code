trigger: none

pool: 220925

steps:
- task: TerraformInstaller@1
  displayName: 'Install Latest Terraform'
  inputs:
    terraformVersion: 'latest'   # automatically install latest stable Terraform version
